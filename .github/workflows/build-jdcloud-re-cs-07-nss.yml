name: Build OpenWrt JDCloud RE-CS-07 (LiBwrt NSS)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'LiBwrt NSS branch'
        required: false
        default: 'k6.12-nss'
        type: choice
        options:
          - 'k6.12-nss'
          - 'k6.6-nss'
  push:
    branches:
      - main
    paths:
      - 'configs/jdcloud-re-cs-07-nss.config'
      - '.github/workflows/build-jdcloud-re-cs-07-nss.yml'

permissions:
  contents: write

env:
  REPO_URL: https://github.com/LiBwrt/openwrt-6.x
  REPO_BRANCH: k6.12-nss
  CONFIG_FILE: configs/jdcloud-re-cs-07.config
  TARGET_NAME: jdcloud-re-cs-07-libwrt-nss
  PORTING_BRANCH: libwrt-jdcloud-re-cs-07-nss
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai
  # 缓存版本号 - 如果依赖出问题可以手动修改此版本号强制刷新缓存
  CACHE_VERSION: v1

jobs:
  build:
    runs-on: ubuntu-24.04
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Free Disk Space
      run: |
        echo "清理前磁盘空间:"
        df -h
        
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        
        echo "清理后磁盘空间:"
        df -h
        
    - name: Initialize Environment
      run: |
        sudo timedatectl set-timezone "$TZ"
        bash scripts/ci_common.sh install_deps
        
    - name: Clone LiBwrt Source
      run: |
        echo "🚀 克隆 LiBwrt/openwrt-6.x ($REPO_BRANCH)..."
        git clone --single-branch -b "$REPO_BRANCH" "$REPO_URL" openwrt
        
        cd openwrt
        echo "✅ 源码信息:"
        echo "  分支: $(git branch --show-current)"
        echo "  提交: $(git log -1 --oneline)"
        echo "OPENWRT_COMMIT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
        
    - name: Restore DL Cache
      uses: actions/cache/restore@v4
      with:
        path: openwrt/dl
        key: dl-${{ env.REPO_BRANCH }}-${{ env.CACHE_VERSION }}-${{ hashFiles('openwrt/feeds.conf.default') }}
        restore-keys: |
          dl-${{ env.REPO_BRANCH }}-${{ env.CACHE_VERSION }}-
          
    - name: Restore Go Module Cache
      uses: actions/cache/restore@v4
      with:
        path: openwrt/dl/go-mod-cache
        key: go-mod-${{ env.REPO_BRANCH }}-${{ env.CACHE_VERSION }}-${{ hashFiles('openwrt/feeds/packages/net/adguardhome/Makefile') }}
        restore-keys: |
          go-mod-${{ env.REPO_BRANCH }}-${{ env.CACHE_VERSION }}-
          
    - name: Restore Staging Cache
      uses: actions/cache/restore@v4
      with:
        path: |
          openwrt/staging_dir/host
          openwrt/staging_dir/hostpkg
          openwrt/staging_dir/toolchain-*
        key: staging-${{ env.REPO_BRANCH }}-${{ env.CACHE_VERSION }}-${{ env.OPENWRT_COMMIT }}
        restore-keys: |
          staging-${{ env.REPO_BRANCH }}-${{ env.CACHE_VERSION }}-
        
    - name: Push Source to Porting Branch
      run: |
        cd openwrt
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git remote add current "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
        git fetch current || true
        
        echo "📤 推送源码到分支: ${{ env.PORTING_BRANCH }}"
        git push current HEAD:${{ env.PORTING_BRANCH }} --force
        
    - name: Generate and Commit Documentation
      run: |
        bash scripts/generate-readme-libwrt.sh "${{ github.server_url }}/${{ github.repository }}" "${{ env.PORTING_BRANCH }}"
        
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add porting/ || true
        git diff --cached --quiet || git commit -m "Update LiBwrt NSS documentation [skip ci]"
        git push origin HEAD:${{ github.ref_name }} || true
        
    - name: Setup Feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a
        
    - name: Load Configuration
      run: |
        cd openwrt
        if [ -f "../$CONFIG_FILE" ]; then
          echo "📋 使用自定义配置"
          cp ../$CONFIG_FILE .config
        else
          echo "⚠️ 使用默认配置"
          echo "CONFIG_TARGET_qualcommax=y" > .config
          echo "CONFIG_TARGET_qualcommax_ipq60xx=y" >> .config
          echo "CONFIG_TARGET_qualcommax_ipq60xx_DEVICE_jdcloud_re-cs-07=y" >> .config
          echo "CONFIG_PACKAGE_luci=y" >> .config
        fi
        make defconfig
        
    - name: Verify Go Module Cache Integrity
      run: |
        cd openwrt
        echo "🔍 检查 Go 模块缓存完整性..."
        
        # 如果 go-mod-cache 存在但损坏，则清理
        if [ -d "dl/go-mod-cache" ]; then
          echo "检测到现有 Go 模块缓存"
          
          # 检查缓存是否损坏（查找常见损坏标志）
          if find dl/go-mod-cache -name "*.partial" -o -name "*.tmp" | grep -q .; then
            echo "⚠️ 发现损坏的缓存文件，清理中..."
            rm -rf dl/go-mod-cache
          else
            echo "✅ Go 模块缓存看起来完整"
          fi
        fi
        
        # 清理可能的临时构建缓存
        rm -rf tmp/go-build
        
    - name: Download Dependencies
      run: |
        cd openwrt
        echo "📥 下载依赖包..."
        
        # 第一次尝试并行下载
        if ! make download -j$(nproc) V=s; then
          echo "⚠️ 并行下载失败，使用单线程重试..."
          make download -j1 V=s
        fi
        
        # 清理小于 1KB 的文件（通常是下载失败的占位符）
        find dl -size -1024c -exec rm -f {} \;
        
        # 验证关键 Go 模块是否下载完整
        if [ -d "dl/go-mod-cache" ]; then
          echo "✅ Go 模块缓存目录大小: $(du -sh dl/go-mod-cache | cut -f1)"
        fi
        
    - name: Build Firmware
      run: |
        cd openwrt
        echo "🔨 开始编译..."
        echo "使用 $(($(nproc) + 1)) 个线程..."
        
        # 并行编译，失败后单线程详细输出
        if ! make -j$(($(nproc) + 1)) V=w; then
          echo "⚠️ 并行编译失败，使用单线程详细模式重试..."
          make -j1 V=s
        fi
        
    - name: Show Build Stats
      if: always()
      run: |
        cd openwrt
        echo "📊 构建目录大小:"
        du -sh build_dir dl staging_dir tmp bin 2>/dev/null || true
        
        if [ -d "dl/go-mod-cache" ]; then
          echo "📊 Go 模块缓存大小: $(du -sh dl/go-mod-cache | cut -f1)"
          echo "📊 Go 模块数量: $(find dl/go-mod-cache -type f | wc -l)"
        fi
        
        echo "📊 磁盘使用:"
        df -h
        
    - name: Save DL Cache
      uses: actions/cache/save@v4
      if: success()
      with:
        path: openwrt/dl
        key: dl-${{ env.REPO_BRANCH }}-${{ env.CACHE_VERSION }}-${{ hashFiles('openwrt/feeds.conf.default') }}
        
    - name: Save Go Module Cache
      uses: actions/cache/save@v4
      if: success()
      with:
        path: openwrt/dl/go-mod-cache
        key: go-mod-${{ env.REPO_BRANCH }}-${{ env.CACHE_VERSION }}-${{ hashFiles('openwrt/feeds/packages/net/adguardhome/Makefile') }}
        
    - name: Save Staging Cache
      uses: actions/cache/save@v4
      if: success()
      with:
        path: |
          openwrt/staging_dir/host
          openwrt/staging_dir/hostpkg
          openwrt/staging_dir/toolchain-*
        key: staging-${{ env.REPO_BRANCH }}-${{ env.CACHE_VERSION }}-${{ env.OPENWRT_COMMIT }}
        
    - name: Organize Firmware
      if: env.UPLOAD_FIRMWARE == 'true'
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        echo "FIRMWARE_PATH=$PWD" >> $GITHUB_ENV
        echo "FILE_DATE=$(date +"%Y%m%d-%H%M")" >> $GITHUB_ENV
        
    - name: Upload Firmware Artifact
      if: env.UPLOAD_FIRMWARE == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: firmware-libwrt-nss_${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE_PATH }}
        
    - name: Generate Release Info
      if: env.UPLOAD_RELEASE == 'true'
      run: |
        echo "RELEASE_TAG=$(date +"%Y.%m.%d-%H%M")" >> $GITHUB_ENV
        bash scripts/generate-release-libwrt.sh "${{ github.server_url }}/${{ github.repository }}" "${{ env.PORTING_BRANCH }}" "$(date +'%Y-%m-%d %H:%M:%S')"
        
    - name: Upload Release
      if: env.UPLOAD_RELEASE == 'true'
      uses: softprops/action-gh-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ env.TARGET_NAME }}_${{ env.RELEASE_TAG }}
        name: LiBwrt NSS ${{ env.RELEASE_TAG }}
        body_path: release.txt
        files: ${{ env.FIRMWARE_PATH }}/*